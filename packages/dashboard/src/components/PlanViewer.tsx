/**
 * PlanViewer component — displays a generated plan.
 */
export const PlanViewer = (): JSX.Element => {
  return (
    <div className="plan-viewer">
      <h2>Plan Viewer</h2>
      <p>No plan loaded.</p>
    </div>
  );
};

