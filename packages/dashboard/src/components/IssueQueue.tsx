/**
 * IssueQueue component — displays a list of issues being processed.
 */
export const IssueQueue = (): JSX.Element => {
  return (
    <div className="issue-queue">
      <h2>Issue Queue</h2>
      <p>No issues in queue.</p>
    </div>
  );
};

