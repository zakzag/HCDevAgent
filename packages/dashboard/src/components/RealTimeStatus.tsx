/**
 * RealTimeStatus component — displays real-time agent status via WebSocket.
 */
export const RealTimeStatus = (): JSX.Element => {
  return (
    <div className="real-time-status">
      <h2>Agent Status</h2>
      <p>Connected</p>
    </div>
  );
};

