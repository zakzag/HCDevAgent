make a plan to improve investigator. I want investigator module in agent package
- decide whether human interaction is needed to fix the issue. if not, fix it itself, and comment
- use more context, like project description and some important code
- add a new entry to
- create a new folder, called .agent and put a project description file in it, which will be the context for the jira issue investigator
- add important code to the context as well
- add a config folder to the root, which has a config file in it, that has several settings for each module